project Adatftpd_ANET is

   for Languages use ("Ada");
   for Source_Dirs use ("src/**");
   for Object_Dir use "obj";
   for Exec_Dir use "test";
   for Main use ("main.adb");

   package Builder is
   end Builder;

   package Compiler is
      for Switches ("ada") use ("-gnata");
   end Compiler;

   package Naming is
      for Spec ("socket_layer") use "socket_layer-anet.ads";
      for Body ("socket_layer") use "socket_layer-anet.adb";
   end Naming;

   package Pretty_Printer is
      for Default_Switches ("ada") use ("-N");
   end Pretty_Printer;

end Adatftpd_ANET;

